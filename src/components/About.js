import React from 'react'

export const AboutUsWho = (props ) => {
    return (
        <h1>{props.title}</h1>
    )
}

export const AboutUsWhere = (props ) => {
    return (
        <h1>{props.city}</h1>
    )
}